# プロジェクト「Html Normalizer」の問題点分析

## 概要

このドキュメントは、VSCode拡張機能「Html Normalizer」のコードベースを多角的に検証し、特定された問題点をまとめたものです。全体として、この拡張機能は「ブラウザのようにHTMLを解釈し、自動修正する」という優れたコンセプトを持っていますが、実装の堅牢性、テスト、エラーハンドリングの観点で多くの課題を抱えています。

## 問題点一覧

### 1. テストの欠如 (深刻度: 高)

- **現象:** プロジェクトには、中核機能であるHTML正規化ロジックを検証するための単体テストや統合テストが一切存在しません。`src/test/extension.test.ts` には、テンプレートが生成したままのサンプルテストしか記述されていません。
- **影響:**
    - バグの混入に気づくことができず、品質が保証されません。
    - リファクタリングや機能追加を安全に行うことができません。
    - 仕様がコード内にしかなく、どのような入力に対してどのような出力を期待するかが不明確です。

### 2. 脆弱なHTML種別判定ロジック (深刻度: 高)

- **現象:** 完全なHTMLドキュメントか、部分的なHTMLスニペットかを `srcHtml.includes("<html")` という文字列検索のみで判定しています。
- **影響:**
    - タグが大文字 (`<HTML>`) の場合に誤判定します。
    - コメント `<!-- <div>...</html> -->` や、スクリプト内の文字列 `const tag = "<html";` などに反応してしまい、意図しない処理分岐に入ってしまう可能性があります。

### 3. 信頼性の低い正規表現によるDOM操作 (深刻度: 高)

- **現象:** 完全なHTMLを処理する際、`jsdom`でパースしたDOMの `innerHTML` を、正規表現を使って元のテキストに再挿入しています。
- **影響:**
    - `<body class="main">` のようにタグに属性が付与されている場合や、予期せぬ空白がある場合に正規表現がマッチせず、置換に失敗する可能性があります。
    - 元のHTMLに `<head>` や `<body>` が存在しない場合、正規表現がマッチしないため、`jsdom`が補完した内容が反映されません。

### 4. 部分的なHTMLの正規化処理の欠陥 (深刻度: 中)

- **現象:** 部分的なHTMLを処理する際、`jsdom`が自動的に付与する `<html><head></head><body>` と `</body></html>` というラッパーを、ハードコードされた文字列置換で削除しています。
- **影響:**
    - 将来的に `jsdom` のバージョンアップでラッパーの出力形式が少しでも変わった場合（例: `<head />`）、この処理は破綻します。
    - ユーザーが `<meta>` タグのリストなど、`<head>` 内で使われることを想定したHTMLスニペットを正規化しようとすると、意図しない結果になります。

### 5. エラーハンドリングの欠如 (深刻度: 中)

- **現象:** `jsdom`でのパース処理など、失敗する可能性のある処理が `try...catch` で囲まれていません。
- **影響:**
    - 整形不可能なほど壊れたHTMLを入力すると、拡張機能全体がクラッシュし、ユーザーには何が起きたか分かりません。
    - `outputChannel` が作成されていますが、エラーメッセージの表示などに一切利用されておらず、宝の持ち腐れになっています。

### 6. 不正確な変更検出ロジック (深刻度: 低)

- **現象:** 正規化処理の前後で変更があったかを判定するために、すべての空白文字 (`\s`) を削除して文字列比較を行っています。
- **影響:**
    - `<b>hello</b>` と `<b>h ello</b>` のように、意味的に異なるHTMLが「変更なし」と判定されてしまう可能性があります。
    - パフォーマンス的にも、巨大なファイルに対して非効率です。

### 7. 設定項目の不足 (改善提案)

- **現象:** インデントのスタイル（スペースかタブか、その数など）や、正規化のルールをユーザーがカスタマイズできる設定項目がありません。
- **影響:**
    - ユーザーやプロジェクトのコーディングスタイルに合わせた整形ができず、利便性が損なわれています。

### 8. 依存関係の健全性 (要確認)

- **現象:** `package.json` に記載されている依存関係（特に `jsdom`）が最新であるか、また既知の脆弱性が含まれていないかどうかが確認されていません。
- **影響:**
    - 古いバージョンのライブラリを使い続けることによる、パフォーマンスやセキュリティ上のリスクがあります。

## 総合的な評価と推奨事項

この拡張機能は、多くの開発者が求めるであろう便利な機能を提供しようとしています。しかし、現在の実装はエッジケースに弱く、非常に不安定であると言わざるを得ません。

最も優先すべきは、**テストスイートの構築**です。堅牢なテストケース群を整備することで、初めて安心してリファクタリングやバグ修正に着手できます。テストを整備した後、正規表現に依存した処理をより安全なDOMベースの操作に置き換え、エラーハンドリングを強化することを推奨します。
