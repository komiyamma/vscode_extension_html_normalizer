
# Html Normalizer の現状と課題

## 概要

このドキュメントは、VSCode拡張機能「Html Normalizer」の現状と、今後の改善ポイントをまとめたものです。  
本拡張機能は「ブラウザのようにHTMLを解釈し、自動修正する」というコンセプトを実現し、主要な問題点の多くは既に解消されています。

## 主な解消済み課題

- **テストの充実:**  
    コア機能の単体テスト・統合テストが追加され、主要なケース（コメント・スクリプト・テーブル・部分HTML・完全HTMLなど）を網羅しています。

- **HTML種別判定の堅牢化:**  
    `<html>`タグの判定は、コメントやスクリプト内の文字列を除外し、タグの大文字・小文字も考慮するよう改善されています。

- **DOMベースの操作:**  
    正規表現による脆弱な置換処理は廃止され、jsdomによるDOM操作とシリアライズに統一されました。

- **部分HTMLの扱い:**  
    jsdomのラッパー除去はハードコードではなく、フラグメント直列化により安定して部分HTMLを扱えるようになりました。

- **エラーハンドリング:**  
    パース失敗時のtry-catchや、Output Channelによるエラーメッセージ表示が実装されています。

## 今後の改善ポイント

- **設定項目の追加:**  
    インデントスタイルや正規化ルールのカスタマイズ機能は未実装です。今後、ユーザーごとのコーディングスタイルに合わせた整形オプションの追加が望まれます。

- **依存関係の健全性:**  
    jsdomなど主要ライブラリのバージョン管理・脆弱性チェックは継続的に行う必要があります。

- **変更検出ロジック:**  
    空白除去による比較は改善されつつありますが、より意味的な差分検出が理想です。

- **【要改善】変更比較ロジックの脆弱性:**
    現在の変更比較は、すべての空白文字を区別なく削除するため、`"Hello World"`と`"HelloWorld"`のような意味的に異なるテキストを「変更なし」と誤判定します。タグ間のインデントや改行のみを無視し、テキストノード内の意味のあるスペースは保持する、より精密な比較ロジックへの修正が求められます。

- **【要改善】ユーザー体験を損なう全体置換:**
    正規化の適用時にエディタのテキスト全体を置換しているため、ユーザーのカーソル位置や選択範囲がリセットされます。差分検出を導入し、最小限の変更箇所だけを適用することで、よりスムーズなユーザー体験を目指すべきです。

- **【要改善】HTML断片の不安定な手動シリアライズ:**
    部分HTMLを文字列に戻す処理が手動で実装されており、特殊なノード（例: CDATA）や文字エスケープが不完全になる可能性があります。`jsdom`が提供する、より堅牢なシリアライズ機能の活用を検討すべきです。

- **【要改善】`any`型の多用による型安全性の低下:**
    コードベースで`any`型が多用されており、TypeScriptの静的型チェックの恩恵を十分に受けられていません。`jsdom`の型定義を厳密に適用するか、カスタムインターフェースを定義することで`any`を撲滅し、コードの信頼性を高めるべきです。

## 総合評価

Html Normalizerは、日常的なHTML編集において「タグ抜け・構造崩れ」を自動修正できる実用的な拡張機能へと進化しています。  
今後は、ユーザーごとの細かなニーズに応えるための設定機能や、さらなる安定性向上を目指して開発を続けていきます。
